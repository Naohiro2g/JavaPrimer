（第n回）実技試験課題#8 2023-01-12  task_08.java     
     
課題：楽天データとlibSVM形式への変換

以下のJavaプログラムを作成せよ

準備
配布した楽天データファイルをダウンロードして保存する
データファイルの説明文書も同時に配布しているので、説明を読む(IN/data_spec_20160311.txt)
IN/ichiba04_review201001_20140221-sample.tsv

 - 1行分が1レビューに相当する


(1) レビュー１つを表すクラスを定義せよ
ReviewT8.java
    (1-1) 説明にある全項目に対応するprivateメンバー変数をそれぞれ定義せよ（それぞれ適切な型を割り振ること）
    (1-2) 1-1で定義したメンバー変数に代入して初期化するコンストラクタを作成せよ
    (1-3) 各メンバー変数についてgetterを作成せよ

(2)mainメソッドを定義して、その中で
    (2-1) 先に作成したクラス型の配列を一つ定義して初期化せよ
    (2-2) 保存した楽天データファイルを読み込み
         - 1行ごとに、作成したクラスを new で初期化する
         - Stringクラスのインスタンスに対しsplit(“\t”)を呼び出せば、タブ区切りの結果を配列で返してくれる
         - 初期化したインスタンスを配列に格納する
         - その際、添え字が必要になるので、読み込み中の行数を表すカウンタ変数を定義するのが良い
    (2-3) LibSVM形式で出力し適当なファイルに保存せよ
         - 特徴量として「年齢」「性別」から得られる情報を用いること
         - ラベルとして評価ポイントを出力する。ただし、3以上か2以下かの二値に変換すること
         - LibSVM形式はたとえば　1 1:87 2:0.5 … といった形の行がならんだもの
         - ラベル 特徴量番号:特徴量 ….


ここまでが「基本試験」
以下は「発展試験」です


（発展1）基本試験で生成したlibSVM形式のファイルを用いて、以下のコマンド実行のコンソール出力（実行したコマンドと結果）をテキストファイルに保存して提出する
(1-1) libSVMで訓練・学習を実行し、評価値を出力する
(1-2) 交差検定も実行し、 評価値を出力する
(1-3) ハイパーパラメータ（g,c）を変更するグリッドサーチを行い、 評価値を出力する
         - libSVM付属の grid.py を利用してよい
ただし、libSVMはこれまでの演習でコンパイル（make）済みのものを使用する

（発展2）上記のコマンド実行を、Javaから外部プロセスとして呼び出せるようにし、データの読み込み・書き出しから訓練・評価まで一括して実行できるようにせよ
         - 基本試験のJavaプログラムに含めること

発展1は、コンソール（ターミナル）からのコマンド実行で行う。
発展2は、外部プロセス呼び出しを活用し、Javaプログラム一本で実行できるようにまとめる。
